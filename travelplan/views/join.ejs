<!DOCTYPE HTML>
<html>

  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Goldilocks Web service</title>
    <script src="/js/jquery-latest.min.js" type="text/javascript"></script>
    <script>
      function submitJoin(option) {
          var id = $('#id').val();
          var passwd = $('#passwd').val();
          var email = $('#email').val();
          if(option == 'add') {
              // 새 글 등록 시
              if(id == '' || passwd == '' || email == '') {
                  alert("모두 입력하세요");
                  return;
              } else {
                  $('#join_form').submit();
              }
          }
      }
    </script>

    <style>
      a { text-decoration:none }
      a:hover { text-decoration:none !important }
    </style>
  </head>

  <body style="margin: 0 0 0 0;" onLoad="document.join_form.id.focus();">
    <div id="join_bg">
      <div class="join_logo"><img src="/img/img_intro.png" width="286" height="70"></div>
      <div class="join_box">
        <div class="box_title">
          <div class="title_txt">Join</div>
          <form method="POST" action="/join" name="join_form" id="join_form">
            <div>아이디 <input type="text" class="input_type" name="id" id="id"/></div>
            <div>비밀번호 <input type="password" class="input_type" name="passwd" id="passwd" /></div>
            <div>비밀번호 확인<input type="password" class="input_type" name="passwd2" id="passwd2" /></div>
            <div>이메일 <input type="text" class="input_type" name="email" id="email" onkeypress="if(event.keyCode==13) {document.forms['join_form'].submit();}"/></div>
          </form>
          <div id="div_join"><input type="button" name="btn_join" id="btn_join" value="Join" onclick="submitJoin('add')"></div>
        </div>
      </div>
    </div>
  </body>
</html>
